from setuptools import setup

PLUGIN_NAME="cm_plugin_list_libraries"

setup(
    name=PLUGIN_NAME,
    packages=[PLUGIN_NAME],
    version="0.0.5",
    description="A plugin that allow list all configured libraries.",
    url="www.example.com",
)
